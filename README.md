# Dyanomdb
In folder Dynamodb there is a small app which is jwt token base REST API endpoint for CRUD operation on dynamodb.
# lambda Function
it contain for lambda fuction which trigger on changes in s3 and insert data in dynamodb
# terraform 
it contain terraform script which create dynamodb table , iam role and lambda function with s3 trigger and s3 bucket.
